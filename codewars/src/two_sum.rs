use std::collections::HashSet;
use std::collections::HashMap;
struct Solution;
impl Solution {
    pub fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {
        // let mut set: HashSet<i32> = HashSet::new();
        // let set:HashSet<i32> = HashSet::from_iter(nums.iter().cloned());
        // let map:HashMap<i32, u32> = HashMap::from_iter(nums.iter().enumerate().cloned());

        // for i in & nums. {
        //     if set.contains(i) {

        //     }

        // }
        // nums.iter().enumerate().for_each(|i, v| {

        // })
        for (i, v) in &nums {

        }
        // for i in nums {
        //     set.insert(i);
        // }
        return Vec::new();
    }
}
